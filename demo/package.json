{
  "name": "fairygui-babylon-demo",
  "private": true,
  "type": "module",
  "description": "Interactive viewer for FairyGUI packages, rendered by fairygui-babylon on Babylon.js.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@babylonjs/core": "^9.26.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.3.0"
  }
}
